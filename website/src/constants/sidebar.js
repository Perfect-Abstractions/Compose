/**
 * LocalStorage keys for persisted sidebar visibility (docs and blog).
 * Used with useSidebarVisibility; toggle UI lives in breadcrumbs.
 */
export const STORAGE_KEYS = {
  docs: 'compose-docs-sidebar-hidden',
  blog: 'compose-blog-sidebar-hidden',
};
